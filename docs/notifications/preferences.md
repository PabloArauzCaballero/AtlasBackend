# Notification preferences

Tabla: `user_notification_preferences`.

Permite activar/desactivar canales por usuario, evento y canal.

Si `is_required = true`, la preferencia no se puede desactivar mediante configuración normal. Esto se usa para comunicaciones operativas o críticas.
