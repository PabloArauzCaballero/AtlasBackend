# Notification templates

Tabla: `notification_templates`.

Permite versionar textos por:

- `code`
- `channel`
- `locale`
- `version`

El renderer soporta placeholders simples:

```txt
Hola {{customerName}}, tu cuota vence el {{dueDate}}.
```
