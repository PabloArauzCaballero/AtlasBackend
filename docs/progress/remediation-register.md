# Registro de remediación — AtlasBackend

Fuente de verdad de hallazgos abiertos del `PLAN_ACCION_ATLAS_BACKEND_10_10.md` y su revisión
`PLAN_ACCION_ATLAS_BACKEND_10_10_ACTUALIZADO.md` (IDs con prefijo `ATLAS-P0-SEC`/`ATLAS-P0-SMOKE`/
`ATLAS-P0-AUTH`/`ATLAS-P1-*` vienen del plan actualizado). Solo se listan paquetes evaluados contra
el código real (no se asume nada del plan sin verificarlo primero).

Estados: `OPEN` · `IN_PROGRESS` · `CODE_COMPLETE` · `VERIFIED_LOCAL` · `VERIFIED_CI` · `CLOSED` · `ACCEPTED_RISK` · `DEFERRED`

| ID | Severidad | Área | Estado | Archivos | Evidencia |
|---|---|---|---|---|---|
| ATLAS-P0-003 | Crítica | Secretos en artefactos | CLOSED | `scripts/smoke/http.ts`, `.gitignore` | `smoke-results.json` desligado de git (`git rm --cached`), redacción por clave (`password`, `accessToken`, `refreshToken`, etc.) y por patrón JWT agregada en `writeSmokeResults()`. Verificado con script de sanidad ad-hoc (ver historial de conversación); `tsc --noEmit` y `eslint` limpios. |
| ATLAS-P0-001 | Crítica | Seed de admin en producción | CLOSED | `src/database/seeders/20260704121000-seed-internal-rbac-and-pablo.ts` | `up()` ahora lanza `Error` de inmediato si `env.NODE_ENV === 'production'`, antes de abrir la transacción. `db:migration:status`/`db:seed:status` no se ven afectados; solo bloquea la ejecución real del seeder. |
| ATLAS-P0-002 | Crítica | Credencial admin hardcodeada | CLOSED | mismo seeder que ATLAS-P0-001, `docs/database/dev-credentials.md`, `docs/database/seeds.md`, `.env.example`, `scripts/smoke/internal-rbac.smoke.ts`, `scripts/smoke/frontend-contract.smoke.ts`, `src/modules/systems-ops/systems-seed-fixtures.ts` | Hash Argon2id anterior (`OXSNkeaFiWk4isxmbnnDSPC...`) tratado como comprometido permanentemente por estar en el historial de git — rotado a un hash nuevo generado con `hashPassword()`. La contraseña en texto plano vieja (`Atlas_Pablo#2026!`) se eliminó de toda la documentación versionada (no se reemplazó por la nueva en texto plano en ningún doc). Los smoke scripts que hacían login real como `pablo` (dependencia funcional real, no solo doc) se actualizaron para usar la nueva contraseña como fallback, igual que el patrón ya existente de `INTERNAL_SMOKE_QA_PASSWORD`. `systems-seed-fixtures.ts` tenía la credencial real filtrada en un `minPayloadSchema` de ejemplo — se corrigió a un placeholder genérico (`'string\|required'`), igual que su fixture hermana `POST_AUTH_LOGIN`. **Aplicado a la base local de desarrollo**: se confirmó vía `db:seed:status` que el seeder ya estaba en `executed` (base sembrada antes de esta rotación). Se corrió un `UPDATE` de un solo uso, dirigido (`WHERE actor_type = 'internal_user' AND actor_id = 1`), que fijó el hash nuevo, incrementó `token_version` (invalida cualquier access token viejo emitido con la contraseña anterior, mismo mecanismo que `logout allDevices`) y limpió `failed_login_attempts`/`locked_until`. `rowCount: 1` confirmado. El script de un solo uso se borró después de ejecutarse; no quedó persistido en el repo. |
| ATLAS-P0-004 / ATLAS-P0-AUTH-001 | Alta | Rotación de refresh token no atómica | CLOSED | `src/modules/auth/auth.service.ts` (`refresh`, `rotateRefreshTokenWithinTransaction`), `src/modules/auth/auth.repository.ts` (`findRefreshTokenForUpdate`, `revokeDescendantChain`, `recordRefreshReuseEvent`, transacción agregada a `createRefreshToken`/`revokeRefreshToken`/`findCredentialsByActor`), `test/unit/auth/auth.service.spec.ts`, `test/unit/auth/auth-repository-email-lookup.spec.ts` | Toda la rotación (leer, validar, revocar el viejo, crear el nuevo) corre en una única transacción con el token bloqueado por `SELECT ... FOR UPDATE`. Reuse detection implementado sin migración nueva: se reutiliza `replaced_by_token_id` (ya existía) como lista enlazada y una CTE recursiva revoca los descendientes todavía activos; al detectarse reuso se llama `TokenRevocationService.bumpTokenVersion` (mismo mecanismo que `logout allDevices`) y se audita en `operational_audit_logs` (`auth.refresh_token.reuse_detected`). Diseño con "resultado discriminado": la transacción de Sequelize siempre resuelve (nunca lanza), y la excepción se lanza recién afuera, después del commit — necesario para que la revocación de la cadena de descendientes por reuso no se deshaga con el rollback automático que Sequelize haría si se lanzara dentro del callback. Tests agregados: token desconocido, expirado, revocado por logout (no confundido con reuso), rotación feliz, actor inactivo, reuso detectado (verifica `revokeDescendantChain` + `recordRefreshReuseEvent` + `bumpTokenVersion`, y que esas escrituras ocurren *antes* de lanzar, no después), y concurrencia real con `Promise.allSettled` sobre un mock de `sequelize.transaction` que encola las llamadas (simula el bloqueo de fila): exactamente una de las dos solicitudes concurrentes con el mismo token tiene éxito, la otra cae en la rama de reuso. 19/19 tests de `auth.service.spec.ts` pasan; suite completa 98/98 suites, 877/877 tests; `tsc --noEmit` y `eslint` limpios en todo el repo. Nota: esta sesión coincidió con trabajo en paralelo de otra persona/sesión sobre un flujo de login-PIN/reset de contraseña en los mismos archivos (`auth.service.ts`, `auth.repository.ts`, `auth.module.ts`) — se integró sin conflicto; de paso se corrigió `test/unit/auth/auth-repository-email-lookup.spec.ts`, que instanciaba `AuthRepository` con argumentos posicionales y quedó desalineado por el nuevo parámetro `oneTimeCodeModel` del constructor (causaba `TypeError` en `findPlatformUserByEmail`, no relacionado con refresh tokens). |
| ATLAS-P0-SEC-001 (continuación) | Crítica | Fallback de contraseña embebido en smoke scripts | CLOSED | `scripts/smoke/required-smoke-env.ts` (nuevo), `scripts/smoke/internal-rbac.smoke.ts`, `scripts/smoke/frontend-contract.smoke.ts`, `.env.example` | `INTERNAL_SMOKE_PASSWORD`/`INTERNAL_SMOKE_QA_PASSWORD` ya no tienen valor por defecto — `requireSmokeEnv()` lanza error claro si falta la variable, antes de cualquier llamada de red (verificado ejecutando el script sin la env var). Ninguno de los dos scripts corre hoy en CI (`ci.yml` solo invoca `smoke:core/auth/sessions/catalog/runtime/risk-telemetry/events/notifications/external-providers:all`, no `smoke:internal-rbac` ni `smoke:frontend-contract`, ni el runner agregado `yarn smoke`), así que este cambio no rompe CI. `tsc --noEmit` y `eslint` limpios. |
| ATLAS-P0-SMOKE-001 | Crítica | Política de JSON de smoke incompleta | CLOSED | `scripts/smoke/http.ts`, `scripts/smoke/redact.ts` (nuevo), `scripts/check-no-tracked-smoke-results.ts` (nuevo), `.gitignore`, `package.json`, `.github/workflows/ci.yml`, `test/unit/smoke/smoke-redaction.spec.ts` (nuevo), `docs/testing/smoke-results.md` (nuevo) | Regla innegociable respetada: `writeSmokeResults()` se conserva y se sigue generando JSON siempre. Cambios: (1) salida movida a `scripts/smoke/results/*.json` con contrato estable (`schemaVersion`, `suite`, `generatedAt`, `commitSha`, `environment`, `summary`, `calls`); (2) escritura atómica (`.tmp` + `rename`); (3) redacción extraída a `redact.ts` (testeable) y ampliada — claves sensibles (+ `otp`/`mfaCode`/`recoveryCode`), JWT completo o embebido, headers `Bearer`, bloques PEM, credenciales en URL; (4) `.gitignore` actualizado, `git rm --cached --ignore-unmatch` corrido (no-op, nada estaba trackeado); (5) gate `yarn check:smoke-results-untracked` (falla si `git ls-files` encuentra algo bajo los patrones prohibidos) agregado a CI antes de lint/type-check/tests; (6) artifact `atlas-smoke-results` subido siempre (`if-no-files-found: warn`). Verificado end-to-end: corrida real de `writeSmokeResults()` con datos de prueba confirmó contrato + redacción correctos; `git check-ignore -v` reconoce la regla; `git ls-files` no devuelve nada; `yarn check:smoke-results-untracked` exit 0; 14/14 tests de redacción pasan (incluye un falso positivo real detectado y corregido: el patrón de escaneo de `Bearer` matcheaba su propio placeholder `[REDACTED]`, corregido con negative lookahead). **Deferred**: hoy solo el runner agregado `yarn smoke` (no los `yarn smoke:X` individuales que corre CI) escribe su propio JSON — escritura por-suite queda como mejora futura, documentada en `docs/testing/smoke-results.md`, para no expandir el alcance de este paquete a ~11 archivos de suite.

| ATLAS-PERF-000 | Media | Higiene de recursos previa al arranque | VERIFIED_LOCAL | `scripts/perf/` (7 archivos), `package.json`, `.github/workflows/ci.yml`, `.gitignore`, `docs/performance/backend/00-prestart-resource-hygiene.md` | Fase previa obligatoria implementada: `prestart:diagnose` / `prestart:cleanup` / `prestart:verify` / `start:clean` / `stop:project`. La pertenencia de un proceso exige **dos señales independientes** (entrypoint conocido de Atlas + cwd real bajo la raíz, leído con `lsof`); con una sola no basta, porque sólo-cwd mataría la shell del usuario y sólo-comando mataría el backend de otro repositorio. Los procesos `unknown` nunca se terminan: se reportan y bloquean el arranque. Escalado `SIGTERM` → espera → `SIGKILL`, árbol de hoja a raíz. Verificado en vivo el 2026-08-06 sobre macOS con procesos reales: (1) proceso propio con hijo → árbol completo terminado, sin huérfanos; (2) `--dry-run` → enumeró y no envió ninguna señal; (3) proceso que ignora `SIGTERM` → escaló a `SIGKILL` tras 2008 ms; (4) proceso ajeno con cwd `/private/tmp` ocupando el puerto 3005 → clasificado `foreign`, **no** se tocó, `verify` salió con código 3. Corregido durante la verificación un fallo de parseo de `df` en macOS (columnas `iused`/`ifree` intercaladas leídas como bytes disponibles → `df -k -P`). `diagnose` + `verify` añadidos a CI, que además son su única cobertura automatizada en Linux. |
| ATLAS-PERF-001 | Media | Arnés de baseline y carga | VERIFIED_LOCAL | `scripts/perf/load.ts`, `scripts/perf/lib/{load-engine,load-flows,metrics-probe,budget}.ts`, `config/performance-budget.json`, `docs/performance/backend/{01,05}` | Generador de carga de **modelo abierto** (llegadas a ritmo fijo): el modelo cerrado de `scripts/stress/` baja la carga sola cuando el servidor se ralentiza y produce un p95 optimista, así que no sirve para medir latencia bajo carga — `stress/notifications` se conserva sin cambios porque el modelo cerrado sí es el correcto para verificar que un pipeline procesa todo lo encolado. Seis escenarios, mezcla ponderada de lectura con rutas verificadas contra controladores reales, warm-up descartado, `maxInFlight` con reporte de descartes, y lectura de `/metrics` al inicio y al final para poder atribuir la latencia a pool/heap/event loop. Presupuesto partido: umbrales de **correctitud** (5xx, timeouts, tasa de error) aplicados desde ya; umbrales de **latencia** provisionales que reportan sin fallar mientras `calibratedFrom` sea `null` — un p95 inventado que rompe el build enseña a ignorar el gate. Verificado end-to-end contra backends simulados: corrida limpia (ritmo 2.04 req/s medidos sobre 2 nominales, mezcla respetada, heap y pool leídos) y corrida con 5xx (exit 1, umbrales aplicados nombrados). Descartado k6: no está instalado y duplicaría una responsabilidad que el repositorio ya cubre en TypeScript. |
| ATLAS-PERF-003 | Media | Baseline de rendimiento medido | VERIFIED_LOCAL | `docs/performance/backend/01-baseline.md`, `config/performance-budget.json` | Baseline real contra PostgreSQL 16.14 local (cluster aislado, sin tocar la instalación del usuario): 147 tablas migradas + seed `development`, `NODE_ENV=test`, `API_RATE_LIMIT_MAX` elevado. Tres corridas del escenario `baseline` con dispersión máxima del **3.6 %** (p50 8.97/9.15/9.15 ms, p95 14.76/15.29/15.00 ms, p99 16.69/17.01/16.87 ms, 10.01 req/s, 0 % de error). Escenario `stress` a 150 req/s: p95 **7.42 ms** — la mitad que a 10 req/s, porque a ritmo bajo el proceso paga rutas frías y desoptimización del JIT entre peticiones. Pool en `using=0 waiting=0 size=4` en ambos escenarios: nunca se acercó a `DB_POOL_MAX=20`. El presupuesto se deja **deliberadamente sin calibrar** (`calibratedFrom: null`) pese a existir baseline: el dataset tiene tablas casi vacías y convertir esos percentiles en umbrales bloqueantes daría un gate que pasa siempre en local y falla contra datos reales; los valores quedan como `measuredReference`, que son datos de comparación, no umbrales. |
| ATLAS-PERF-004 | Media | Logging de SQL en desarrollo (PII + contaminación de medición) | OPEN | `src/config/database.config.ts:75` | Hallazgo que **sólo apareció midiendo**: con `NODE_ENV=development`, `logging: console.log` vuelca cada sentencia a stdout — **8 MB de log en una corrida de 150 s**, incluidos los `INSERT` completos de `system_action_logs`. Dos consecuencias: (1) invalida cualquier medición hecha con `yarn start:dev`, y el baseline hubo de rehacerse con `NODE_ENV=test`; (2) contradice una regla explícita del proyecto (`.claude/rules/30-security.md`: «Nunca loguear SQL — Sequelize inlinea valores → fuga de PII»), y en un backend KYC eso es nombre, correo y teléfono en claro en stdout y en `Archivo.log`. No corregido: desacoplar el logging de `NODE_ENV` hacia una variable propia desactivada por defecto es un cambio de contrato de configuración y una decisión de seguridad que pide ADR. **No se le atribuye ningún efecto de latencia**: la corrida con logging activo salió aparentemente más rápida, lo que no es creíble, así que la evidencia sostiene el hallazgo de seguridad, no un número. |
| ATLAS-DOC-008 | Alta | `docs:build --strict` roto en la rama | CLOSED | 252 archivos bajo `docs/` (mayoría en `docs/obsidian/`) | El gate de documentación estaba **rojo antes de este trabajo** (verificado construyendo un worktree en HEAD: 702 warnings, todos de la bóveda Obsidian). Causa: los enlaces a código de la bóveda tenían un nivel de escape de menos — desde `docs/obsidian/backend/05-data/entities/` usaban `../../../../src/...`, que normaliza a `docs/src/...`; hacen falta cinco niveles. Estaban rotos también en GitHub y en el IDE, así que corregirlos no es esquivar el gate sino arreglar enlaces muertos. Corregidos **704 enlaces en 252 archivos** con un criterio verificable y no adivinatorio: sólo se tocó un enlace si (a) su destino no existía en disco y (b) añadir niveles de escape lo hacía resolver a un archivo real del repositorio. `yarn docs:build --strict` pasa ahora en verde. |
| ATLAS-PERF-002 | Media | Mapa de riesgos de rendimiento | IN_PROGRESS | `docs/performance/backend/02-bottleneck-map.md`, `03-optimization-plan.md` | Análisis **estático**: cinco riesgos con evidencia `archivo:línea` y método de medición, sin ninguna optimización aplicada — no hay baseline y optimizar sin medir es lo que el procedimiento prohíbe. R-01 (prioridad 1): `DELIVERY_CONCURRENCY = 25` en `notification-broadcast.service.ts:35` excede por sí solo `DB_POOL_MAX = 20` (`env.schema.ts:56`), y el comentario que justifica el tamaño del pool cita esas mismas ~25 entregas como razón; hay otros cuatro techos de fan-out en constantes sueltas que nada obliga a que quepan juntos en el pool. R-02: `decryptSecretEnvelope` llama a AWS KMS sin caché de data keys (`kms-key-provider.ts:80`) dentro de un `Promise.all` sin techo sobre un `findAll` (`notifications.repository.ts:506`) → N llamadas de red paralelas por destinatario con KMS activo. R-03: ingesta de hasta 500 ítems con round trips secuenciales dentro de una transacción. R-04: `Promise.all` sin límite leyendo fuentes desde un handler HTTP. R-05: interacción entre `statement_timeout` (60 s) y `REQUEST_TIMEOUT_MS` (30 s), diseño correcto con riesgo residual de dimensionamiento. **Actualizado tras medir (ATLAS-PERF-003): R-01 quedó REFUTADO en la ruta de lectura** — a 150 req/s el pool se mantuvo en `waiting=0, size=4`, así que la prioridad 1 del análisis estático no era un cuello en el camino que se pudo medir; sigue abierto sólo para la ruta de broadcast, que la mezcla de lectura nunca recorre. Es el valor concreto de exigir baseline: sin él se habría «optimizado» lo que no era el problema. Próximo paso: Fase A' del plan — dataset representativo y una mezcla que recorra escritura y broadcast, sin lo cual R-01/R-02/R-03 no son comprobables. |

| ATLAS-SEC-007 | Alta | Escalada de privilegios entre tenants | VERIFIED_LOCAL | `src/modules/auth/auth.controller.ts`, `src/modules/auth/auth.service.ts`, `test/unit/auth/auth.service.spec.ts`, `test/unit/auth/auth.controller.spec.ts`, `scripts/smoke/user-types.smoke.ts` | **Explotado en vivo antes de corregir** (`docs/audit/evidence/live-exploit-2026-08-06.md`): un `admin` del tenant 1 llamó `POST /auth/provision-credentials` con el `actorId` de un `internal_user` del tenant 2 sin credenciales, fijó su contraseña y obtuvo un token `{"role":"admin","tenantId":"2"}`. `TenantGuard` no cubre el endpoint porque el destino viaja en el cuerpo. Corregido propagando `tenantId` del token y exigiendo contención en el servicio: `admin` solo dentro de su tenant, `platform_user` solo por `platform_admin`, mismo 403 en ambos casos para no filtrar qué ids existen. Re-ejecutado el mismo ataque: **403**; camino legítimo intacto: **201**. 6 pruebas nuevas + caso de regresión en `smoke:user-types`. |
| ATLAS-SEC-008 | Alta | Segundo factor interno fail-open | VERIFIED_LOCAL | `src/config/env-cross-checks.ts`, `src/modules/auth/auth-second-factor.service.ts` (nuevo), `src/modules/auth/auth.service.ts`, `test/unit/config/env-cross-checks.spec.ts`, `test/unit/auth/auth.service.spec.ts` | `isSecondFactorRequired` devolvía `false` sin canal de correo y ninguna validación exigía MailSender en producción: `admin`/`platform_admin` entraban con solo contraseña, con 200 y sin rastro (verificado en vivo). Dos capas: producción no arranca sin `MAILSENDER_BASE_URL` ni con `AUTH_LOGIN_PIN_ENABLED=false`; y `assertSecondFactorDeliverable` responde 503 si el canal cae en caliente, en vez de emitir tokens de un solo factor. La política se extrajo a `AuthSecondFactorService` (mismo criterio que `AuthActorResolverService`). 6 pruebas nuevas. |
| ATLAS-SEC-009 | Alta | Fuga cross-tenant en el portal interno | VERIFIED_LOCAL | `src/modules/internal-portal/application/portal-scope.util.ts` (nuevo), `portal-operations.service.ts`, `portal-data-quality.service.ts`, `portal-reports.service.ts`, `portal-query.base.ts`, `internal-portal.controller.ts`, `test/unit/internal-portal/portal-tenant-scope.spec.ts` (nuevo) | `system_job_runs` y `data_quality_issues` llevan `_tenant_id` y se consultaban sin filtrarlo. **Verificado en vivo**: lectura de una corrida de job del tenant 2 desde el tenant 1, y **escritura** (`acknowledge`) sobre una alerta del tenant 2. `PortalScope` acota cada caso de uso; `admin` NO recibe alcance global (es rol de tenant por el colapso de roles legacy); el `UPDATE` lleva el tenant en su propio `WHERE` (sin ventana TOCTOU) y responde 404 al no afectar filas. Re-verificado: 0 ocurrencias del job ajeno, 404 en el acknowledge, alerta ajena intacta (`open`). 12 pruebas nuevas. |
| ATLAS-OPS-011 | Media | Endpoints que reportan acciones no ejecutadas | VERIFIED_LOCAL | `internal-portal.controller.ts`, `portal-operations.service.ts`, `portal-reports.service.ts`, `portal-governance.service.ts`, `portal-data-quality.service.ts`, `portal-report-definitions.ts`, `test/unit/internal-portal/internal-portal-service-contract.spec.ts`, `scripts/smoke/frontend-contract.smoke.ts` | Cinco endpoints devolvían 200 describiendo efectos inexistentes: `jobs/:id/retry` (`QUEUED_FOR_RETRY`, el job seguía `completed` — verificado en vivo), `jobs/:id/cancel`, `data-quality/rules/:id/run` (`finishedAt = inicio + 220 ms`), `PATCH governance/policies/:id` (`persisted: false` enterrado) y `reports/:id/snapshots` (dos filas escritas en código con fecha fija). Retirados; las capacidades reales viven en `runtime-jobs`, que sí ejecutan y registran su corrida. Eliminada además la constante `NOW_SEED` (15 fechas inventadas), los `attempts`/`priority` fijos y el disfraz de "export ejecutado". Trinquete en el spec de contrato impide que reaparezcan. |
| ATLAS-SEC-010 | Media | Endpoints sin contrato de entrada | VERIFIED_LOCAL | `src/modules/internal-portal/internal-portal.schemas.ts` (nuevo), `internal-portal.controller.ts` | 15 endpoints del portal usaban `@Query()`/`@Param()`/`@Body()` crudos, únicos en el backend y contra la regla del propio proyecto. Sin inyección (el SQL parametriza) pero sin contrato: el `@ApiQuery` documentaba `minimum/maximum` que nada aplicaba. Todos pasan por `ZodValidationPipe`; verificado en vivo: `limit=9999` → **400** con `issues[]`, `limit=5` → 200. |
| ATLAS-DATA-003 | Media | `outbox_events` sin purga | VERIFIED_LOCAL | `src/modules/runtime-jobs/runtime-maintenance-jobs.service.ts`, `runtime-jobs.controller.ts`, `scheduled-jobs.catalog.ts`, `runtime-jobs.schemas.ts`, `src/config/env.runtime-jobs.schema.ts`, `test/unit/runtime-jobs/runtime-maintenance-jobs.service.spec.ts` | Cierra el hallazgo que quedó abierto en la auditoría del 2026-08-06. Nuevo job `purge_processed_outbox` (HTTP + planificador, `RUNTIME_JOBS_OUTBOX_RETENTION_DAYS=30`). Solo borra `processed` con `processed_at` anterior al corte; `pending`/`processing` nunca se tocan y una fila sin marca de procesado sobrevive para investigarse. Sin el modelo inyectado falla ruidosamente en vez de reportar "0 borrados". 4 pruebas nuevas. |
| ATLAS-DATA-004 | Media | Políticas de retención declaradas y no aplicadas | VERIFIED_LOCAL | `src/modules/runtime-jobs/retention-targets.ts` (nuevo), `runtime-jobs.service.ts`, `scripts/check-retention-coverage.ts` (nuevo), `.github/workflows/ci.yml` | Ocho políticas sembradas (varias en perfil **producción**, con base legal `kyc_aml_record_keeping` y acción `anonymize`) contra tres tablas mapeadas; el resto se reportaba en un campo del JSON del job que nadie leía. No se inventó mapeo —acotar `risk-data-365d` o decidir qué anonimizar es de Riesgo/Legal/Cumplimiento— pero se eliminó el silencio: cada política debe estar mapeada **o** tener escrito qué falta decidir y quién decide. `yarn check:retention-coverage` lo bloquea en CI y descubrió una octava política (`risk-model-inputs-730d`) que ninguna auditoría previa había visto. |
| ATLAS-CI-002 | Media | Gates que aprueban sin comprobar nada | VERIFIED_LOCAL | `scripts/gate-skip-policy.ts` (nuevo), `check-read-api-views.ts`, `check-domain-schema-layout.ts`, `check-db-privileges.ts`, `verify-prod-seed-idempotency.ts` | Cuatro gates imprimían `[skip]` y salían con **código 0** si Postgres no respondía; `check:db-privileges` incluso devolvía `[]` errores. El caso grave no es el portátil sin base sino el job de CI que sí la tiene: una credencial mal puesta convertía la verificación de la matriz de privilegios en un aprobado automático. Ahora fallan cerrado; el salto exige `--allow-skip`/`ATLAS_GATES_ALLOW_SKIP=true`, que CI no pasa. Verificado en ambos sentidos (sin base → exit 1; con `--allow-skip` → exit 0; con base real → verifica de verdad). |
| ATLAS-OPS-012 | Media | `Archivo.log` sin rotación | VERIFIED_LOCAL | `src/common/logging/app-file-logger.service.ts`, `src/config/env.schema.ts`, `test/unit/common/logging/app-file-logger-rotation.spec.ts` (nuevo) | El truncado solo ocurría tras sincronizar a MongoDB; sin Mongo configurado —configuración soportada— el archivo crecía hasta llenar el disco del contenedor, y con el disco lleno se cae el proceso, no solo el logging. Rotación por tamaño (`LOG_FILE_MAX_BYTES`, 64 MB) con un solo relevo: es un búfer hacia el pipeline de logs, no el archivo histórico. 2 pruebas nuevas. |
| ATLAS-SEC-011 | Media | Producción sin KMS asumida por omisión | VERIFIED_LOCAL | `src/config/env-cross-checks.ts`, `src/config/env.schema.ts`, `src/config/env.ts`, `.env.example`, `.env.production.example` | La master key de toda la PII derivada de una env var se advertía con `console.warn` en el arranque, perdido entre las líneas de inicio. Ahora exige declararlo (`PII_ENCRYPTION_ALLOW_ENV_MASTER_KEY=true`): el riesgo queda firmado en el manifiesto y revisable en un PR. Sigue siendo `ACCEPTED_RISK` de producto, pero deja de ser silencioso. |
| ATLAS-TEST-002 | Baja | Errores de tipo en specs como aviso | VERIFIED_LOCAL | `jest.config.cjs`, `package.json`, `.github/workflows/ci.yml` | `ts-jest` corría con `diagnostics.warnOnly: true`: un mock desalineado del contrato real del servicio no rompía la suite. Desactivado (306/306 suites pasan). Además `yarn lint` incorpora `--max-warnings=153`: `complexity`/`max-params`/`max-lines-per-function` estaban declarados como `warn` y CI no los miraba, así que el "gate de complejidad" no bloqueaba nada. |
| ATLAS-SMOKE-002 | Media | `smoke:frontend-contract` inoperante | VERIFIED_LOCAL | `scripts/smoke/frontend-contract.smoke.ts` | Fallaba en el primer paso leyendo `data.accessToken` cuando `/internal/auth/login` entrega el token en cookie `HttpOnly` — el mismo fallo corregido en `user-types.smoke.ts` (`670e9b2`) que aquí quedó pendiente, así que el contrato del Admin Portal **no se comprobaba desde entonces**. Al arreglarlo aparecieron tres desincronizaciones ocultas detrás: un campo `reason` que `updateDataEntityMetadataSchema` (`.strict()`) rechaza, y `/systems/review-queue`, que devuelve seis grupos y no una lista plana (además, una cola vacía es estado legítimo: se verifica la forma, no que haya trabajo pendiente). Primer verde real del smoke. |


| ATLAS-PERF-004 / ATLAS-SEC-012 | Alta | Volcado de SQL con PII en el log | VERIFIED_LOCAL | `src/config/database.config.ts`, `src/config/env.database.schema.ts`, `src/config/env-cross-checks.ts`, `docs/adr/0008-logging-de-sql.md` (nuevo), `.env.example`, `.env.production.example`, `test/unit/config/env-cross-checks.spec.ts` | Cierra el hallazgo que este registro tenía **OPEN**. `logging: env.NODE_ENV === 'development' ? console.log : false` convertía "estoy en desarrollo" en "publica cada sentencia con sus valores inlineados": nombre, correo, teléfono y documento en claro en stdout, en `Archivo.log` y en el espejo de MongoDB que expone `GET /systems/logs/mongo`. Contradecía la regla escrita del propio proyecto (`.claude/rules/30-security.md`). Ahora es `DB_LOG_SQL`, apagada por defecto, **prohibida en producción** por validación cruzada, y cuando se activa pasa por `redactSensitiveText` y el `Logger` de Nest. Decisión razonada en ADR-0008, incluido lo que la redacción **no** resuelve (un valor posicional sin nombre no es reconocible). **Verificado en vivo**: arranque por defecto en `development` → **0** líneas `Executing (default)`; con `DB_LOG_SQL=true` → SQL presente y correos como `[REDACTED_EMAIL]`. |
| ATLAS-DEP-002 | Alta | 11 advisories HIGH en `js-yaml` | VERIFIED_LOCAL | `package.json`, `yarn.lock` | Advisory **publicado durante esta sesión** (GHSA-5p4m-2wfm-xmqj, 2026-08-06 20:27): consumo cuadrático de CPU al resolver `!!omap`, DoS con un documento pequeño, presente en toda la línea 4.x hasta 4.3.0. `js-yaml` es dependencia directa (`@nestjs/swagger`, `eslint` y `jest` también lo arrastran). Subidos el pin directo y el de `resolutions` a `^4.3.1`. `yarn audit --level high` pasa de **11 HIGH** a **0**. |


## Hallazgos del plan ya cerrados antes de este registro (verificado, no requieren trabajo)

- CI (Fase 9): ya existe `.github/workflows/ci.yml` con install → check-env → lint → format → type-check → test+coverage → build, más un job separado con Postgres/Redis reales corriendo migraciones + seeders + smoke en cada PR, más `dependency-audit` (`yarn audit --level high`) y un check de tamaño de migraciones.
- RBAC granular (Fase 3): ya existe `@InternalPermissions`/`InternalPermissionsGuard` (~45 permisos) para `internal-users`; el trabajo pendiente es *extender* cobertura a otros módulos incrementalmente, no crearlo desde cero. Primer paso ya dado: `catalog-management` — endpoints `decision` y `activate` restringidos a `admin`/`platform_admin` (ver commit de esta sesión).

## Fases del plan evaluadas como prematuras o que requieren decisión de infraestructura (no iniciar sin confirmación explícita)

Fases 6 (workers separados), 10 (observabilidad Pino/métricas), 11 (contenedores/backups/despliegue) —
requieren visibilidad sobre el entorno de despliegue real que no está disponible solo desde el
código. No iniciar sin que el usuario confirme la topología de infraestructura actual.
