# Pending items — Proyecto Atlas (Backend)

Este archivo es obligatorio según `PENDIENTES_ATLAS.md` y `PROMPT_MASTER_ATLAS.md`: todo pendiente, supuesto, bloqueo o riesgo debe quedar documentado aquí, no solo mencionado en el chat o en comentarios de código sueltos.

**Estado del proyecto:** Fase 1 (usuarios) en curso, con piezas de Fase 2 (motor de decisión y plataforma administrativa) ya adelantadas. Fase 3 (deudas / BNPL) no iniciada — ver §3.

Última actualización: segunda pasada de cierre de pendientes (2026-07-01), en respuesta directa a que la primera entrega dejaba demasiados ítems como "parcial"/"diferido". Ver §1b para lo que se cerró en esta segunda pasada.

## 1b. Segunda pasada — pendientes que estaban documentados como diferidos y se cerraron

| ID | Estado anterior | Estado ahora | Qué se hizo |
|---|---|---|---|
| ATLAS-PEND-107 (`fraud` no era módulo independiente) | Abierto | **Resuelto** | `src/modules/fraud/` extraído de `operations`/`risk`: `FraudRepository`, `FraudService`, `FraudModule` propios. La ruta HTTP no cambió (`POST /operations/fraud-cases/:caseId/decision`), pero la implementación ya no vive en `OperationsService`. Verificado con `tsc` completo sin errores nuevos, y con `test/unit/fraud/fraud.service.spec.ts` (6 casos). |
| ATLAS-PEND-105 (servicios "god object") | Abierto | **Resuelto para `customer-onboarding.service.ts`** (el más grande y el que tenía el bug de concurrencia); `sessions.service.ts` (~740 líneas) sigue sin descomponer — ver nueva fila ATLAS-PEND-110. | `startOnboarding` (antes ~400 líneas en un solo método) se descompuso en 13 métodos privados de un solo propósito (`createCustomerAndCredentials`, `resolveDeviceAndLink`, `recordConsents`, etc.), cada uno con tipos explícitos derivados de las firmas reales de los repositorios (`Awaited<ReturnType<...>>`). Verificado con `tsc` completo: cero errores nuevos, lo que da alta confianza de que ningún campo/parámetro quedó mal encadenado entre pasos. |
| ATLAS-PEND-104 (Swagger incompleto) | Abierto | **Resuelto (tags); descripciones por endpoint sigue pendiente** | Los 15 controladores preexistentes + `auth` tienen `@ApiTags` agregado (agrupación correcta en Swagger UI/OpenAPI). `@ApiOperation` con descripciones detalladas por endpoint sigue siendo trabajo mecánico pendiente — ver nueva fila ATLAS-PEND-111. |
| ATLAS-PEND-106 (cifrado sin KMS) | Abierto | **Mecanismo listo, no conectado** | Se implementó envelope encryption real (`src/common/utils/crypto/envelope-encryption.util.ts`, `data-key-provider.interface.ts`, `local-key-provider.ts`, `kms-key-provider.ts`) con formato de datos ya compatible con KMS y retrocompatible con el formato `v1` existente. **No se conectó** a `customer-onboarding.service.ts` (que sigue usando `secret-box.util.ts` sin cambios) porque eso requeriría convertir `encryptSecret`/`decryptSecret` de síncronas a asíncronas y tocar cada call site sin poder probarlo contra una base de datos real — ver ATLAS-PEND-112. |
| ATLAS-PEND-103 (cobertura de tests) | Abierto | **Ampliado, sigue parcial** | 2 archivos de test nuevos en esta pasada (`fraud.service.spec.ts`, `envelope-encryption.spec.ts`), sumados a los 5 de la primera pasada. Total 9 archivos de test (antes: 2). Cobertura de los ~13 módulos restantes (customers, sessions, risk, catalog-management, etc.) sigue sin specs propios. |

## 1c. Nuevos pendientes generados por la segunda pasada

| ID | Estado | Tipo | Prioridad | Área | Pendiente | Acción requerida |
|---|---|---|---|---|---|---|
| ATLAS-PEND-110 | Abierto | Deuda técnica | Media | Código | `sessions.service.ts` (~740 líneas) sigue sin descomponerse en pasos, a diferencia de `customer-onboarding.service.ts`. No se tocó en la segunda pasada por límite de tiempo, no por riesgo — el patrón ya está probado y documentado en `customer-onboarding.service.ts` como referencia. | Aplicar el mismo patrón de descomposición en métodos privados tipados. |
| ATLAS-PEND-111 | Abierto | Documentación | Baja | Swagger | Falta `@ApiOperation`/`@ApiResponse` detallados por endpoint en los 15 controladores preexistentes (solo tienen `@ApiTags` a nivel de clase). | Trabajo mecánico, módulo por módulo. |
| ATLAS-PEND-112 | Abierto | Deuda técnica | Media | Cifrado | `envelope-encryption.util.ts` existe y funciona (probado con `test/unit/crypto/envelope-encryption.spec.ts`) pero no está conectado a ningún call site real — `customer-onboarding.service.ts` sigue cifrando teléfono/email con `secret-box.util.ts` (síncrono, clave única reutilizada). Migrar requiere: (1) volver async `createCustomerAndCredentials`/`createContactMethods` en `customer-onboarding.service.ts` — ya son async, así que el cambio real es solo en las llamadas a `encryptSecret`/`decryptSecret`; (2) probar contra una base de datos real que el formato v2 se lee/escribe correctamente; (3) decidir si migrar datos ya cifrados en v1 o dejarlos convivir (el decodificador ya soporta ambos formatos). | Backend, con acceso a una base de datos real para probar antes de desplegar. |



## 1d. Tercera pasada — corrección de arranque local y hardening operativo

| ID | Estado anterior | Estado ahora | Qué se hizo |
|---|---|---|---|
| ATLAS-BOOT-001 (`yarn start:dev` arrancaba como producción si Windows tenía `NODE_ENV=production`) | Abierto por error real de arranque | **Resuelto** | `start:dev` ahora ejecuta `scripts/run-dev.mjs`, que fuerza `NODE_ENV=development` antes de importar `dist/src/main.js`. También se agregó `yarn env:doctor`, `.env.production.example` y documentación del caso exacto de Zod (`REDIS_URL`/`NOTIFICATION_TOKEN_ENCRYPTION_KEY`). |
| ATLAS-SEC-REFRESH-001 (refresh token nuevo antes de validar actor activo) | Abierto | **Resuelto** | `AuthService.refresh()` revalida primero que el actor siga activo y recién después emite el refresh token nuevo. Se agregó test de regresión para confirmar que no queda token huérfano si el cliente está cerrado. |
| ATLAS-RUNTIME-001 (`void` en idempotencia/outbox) | Abierto | **Resuelto** | `IdempotencyInterceptor` y `ApiCommandOutboxInterceptor` ahora esperan la escritura crítica antes de responder. También se preserva status en replay idempotente. |
| ATLAS-AUDIT-HTTP-001 (action log HTTP global) | Pendiente funcional de panel interno | **Resuelto base** | Se agregó `HttpActionLogInterceptor` + `HttpActionLogService` para registrar método, ruta, query sanitizada, actor, IP, user-agent, status, duración, resultado y correlationId en `operational_audit_logs` para todos los endpoints. |
| ATLAS-DOCS-SEC-001 (Swagger default activo en producción) | Riesgo de configuración | **Resuelto** | `API_DOCS_ENABLED` queda activo por defecto solo fuera de producción; en producción debe activarse explícitamente. |

## 1. Pendientes cerrados por este patch (referencia histórica)

| ID | Estado | Tipo | Resumen |
|---|---|---|---|
| ATLAS-AUDIT-002 | Resuelto | Regla técnica | Módulo `auth` (login/refresh/logout/provisión de credenciales) implementado. |
| ATLAS-AUDIT-021 | Resuelto | Regla técnica | Índice único real en `primary_email_hash`; condición de carrera en alta de cliente cerrada. |
| ATLAS-AUDIT-022 | Resuelto | Regla técnica | `processOutbox` usa `FOR UPDATE SKIP LOCKED`, igual que `claimPending`. |
| ATLAS-AUDIT-023 | Resuelto (mecanismo) | Regla técnica | Rate limiting respaldado por Redis cuando `REDIS_URL` está configurado (obligatorio en producción). |
| ATLAS-AUDIT-024 | Resuelto (parcial) | Regla técnica | Job de retención ejecuta purga/anonimización real para 3 tablas de telemetría explícitamente mapeadas. La política `risk-data-365d` sembrada sigue sin mapear (ver ATLAS-PEND-101). |
| ATLAS-AUDIT-025 | Resuelto (parcial) | Regla técnica | Bug de corrección en paginación de auditoría (página ≥2 podía ser incorrecta) cerrado. Cursor real solo aplicado como referencia en `events`; el resto de listados de alto crecimiento sigue en `OFFSET` (ver ATLAS-PEND-102). |
| ATLAS-AUDIT-026 | Resuelto | Regla técnica | `tokenVersion` ahora se valida contra `auth_credentials` en cada request autenticado. |
| ATLAS-AUDIT-027 | Resuelto | Regla técnica | Verificación de ownership unificada en `assertOwnCustomerResource`, con excepciones documentadas donde el comportamiento debía ser distinto (`notifications`, `customer-onboarding`). |
| ATLAS-AUDIT-004 | Resuelto | Seguridad | `.env` real eliminado del paquete; `yarn check:no-env-file` lo impide en CI. |
| ATLAS-AUDIT-005 | Resuelto (parcial) | Regla técnica | Migrado a Jest. Tests nuevos para `auth`, utilidades de ownership/crypto y la regresión de la condición de carrera. Cobertura global de los 15 módulos preexistentes sigue pendiente (ver ATLAS-PEND-103). |
| ATLAS-AUDIT-006 | Resuelto (parcial) | Documentación | Swagger/OpenAPI mecanismo funcionando (`yarn docs:openapi`). Decoración completa de los 15 módulos preexistentes pendiente (ver ATLAS-PEND-104). |
| ATLAS-AUDIT-019 | Resuelto | Regla técnica | CI (`​.github/workflows/ci.yml`) con install/check-env/lint/format/type-check/test/build. |
| ATLAS-AUDIT-020 | Resuelto | Regla técnica | ESLint + Prettier configurados. |

## 2. Pendientes bloqueantes / abiertos nuevos, generados por este mismo patch

| ID | Estado | Tipo | Prioridad | Área | Pendiente | Impacto si no se resuelve | Acción requerida | Responsable | Archivo relacionado |
|---|---|---|---|---|---|---|---|---|---|
| ATLAS-PEND-100 | Abierto | Decisión de negocio | Alta | Auth consumidor | No está decidido si el mecanismo de autenticación definitivo del consumidor final es contraseña, OTP por SMS, o ambos. Este patch implementó contraseña (opcional en onboarding) como base segura razonable, sin cerrar la decisión de producto. | Puede requerir rediseño de UX de login en mobile si la decisión final es OTP-only. | Confirmar con Producto/UX el mecanismo definitivo antes de exponer login por contraseña en la app real. | Producto/Mobile | `src/modules/customer-onboarding/customer-onboarding.schemas.ts`, `src/modules/auth/` |
| ATLAS-PEND-101 | Abierto | Dato faltante | Media | Retención de datos | La política de retención ya sembrada (`risk-data-365d`, `applies_to: risk_and_fraud_testing`) no tiene tabla mapeada en `RETENTION_TARGETS` — su alcance real es ambiguo y podría incluir tablas de decisión/auditoría que no deben purgarse. | Mientras no se mapee, esa política sigue sin ejecutar ninguna acción (comportamiento seguro, pero la retención de esos datos sigue sin resolverse). | Legal/Riesgo debe precisar exactamente qué tablas cubre `risk_and_fraud_testing` antes de mapearla. | Legal/Riesgo | `src/modules/runtime-jobs/runtime-jobs.service.ts` |
| ATLAS-PEND-102 | Abierto | Deuda técnica | Media | Rendimiento | Paginación por cursor real solo se aplicó como referencia en `events` (tabla única). `operations`, `data-quality` y el resto de listados de alto crecimiento siguen en `OFFSET`. El caso de auditoría (`audit.repository.ts`) es un fan-in de 5 tablas heterogéneas: requiere una vista/tabla unificada de eventos (`UNION ALL` + índice compuesto) para tener un cursor real — no es un cambio de una sola función. | Los listados administrativos seguirán degradándose con offsets profundos a medida que crezcan las tablas de telemetría/auditoría, justo cuando Fase 2 los use a diario. | Diseñar la tabla/vista unificada de auditoría; migrar `operations`/`data-quality` al mismo patrón de cursor ya documentado en `src/common/utils/pagination/cursor-pagination.util.ts`. | Backend | `src/modules/audit/`, `src/modules/operations/`, `src/modules/data-quality/` |
| ATLAS-PEND-103 | Abierto | Deuda técnica | Media | Pruebas | Cobertura de Jest agregada por este patch se concentra en código nuevo/corregido de alto riesgo (auth, condición de carrera, ownership). Los 15 módulos preexistentes (customers, sessions, risk, catalog-management, operations, etc.) siguen sin `*.service.spec.ts`/`*.controller.spec.ts` propios. | Sin red de pruebas para la lógica de negocio existente (algunos servicios de 700+ líneas), el riesgo de regresión silenciosa se mantiene alto. | Escribir specs de servicio/controlador módulo por módulo, priorizando `sessions.service.ts` y `customer-onboarding.service.ts` por tamaño/complejidad transaccional. | Backend | `test/unit/**` |
| ATLAS-PEND-104 | Resuelto (parcial, ver §1b) | Deuda técnica | Baja | Documentación de API | `@ApiTags` agregado a los 15 controladores + `auth`. `@ApiOperation` por endpoint sigue pendiente — ver ATLAS-PEND-111. | El contrato OpenAPI ya agrupa correctamente por módulo; falta descripción por endpoint. | Ver ATLAS-PEND-111. | Backend | `src/modules/**/*.controller.ts` |
| ATLAS-PEND-105 | Resuelto para `customer-onboarding.service.ts`, ver §1b | Deuda técnica | Media | Servicios grandes | `customer-onboarding.service.ts` descompuesto en 13 métodos privados tipados. `sessions.service.ts` (~740 líneas) sigue igual — ver ATLAS-PEND-110. | Menor para `customer-onboarding`; se mantiene para `sessions`. | Aplicar el mismo patrón a `sessions.service.ts` (ATLAS-PEND-110). | Backend | `src/modules/sessions/sessions.service.ts` |
| ATLAS-PEND-106 | Mecanismo implementado, no conectado — ver §1b | Riesgo legal/regulatorio | Baja | Cifrado | `envelope-encryption.util.ts` implementa envelope encryption real, listo para KMS, pero no reemplaza a `secret-box.util.ts` en ningún call site todavía — ver ATLAS-PEND-112. | Aceptable para MVP/Fase 1; el mecanismo ya existe para cuando se decida conectar KMS real. | Ver ATLAS-PEND-112 para el plan exacto de conexión. | Backend/Infra | `src/common/utils/crypto/envelope-encryption.util.ts` |
| ATLAS-PEND-107 | **Resuelto** — ver §1b | Regla técnica | — | Riesgo/Fraude | `src/modules/fraud/` extraído con `FraudRepository`/`FraudService`/`FraudModule` propios, `decideFraudCase` movido fuera de `OperationsService`. | Cerrado. | — | Backend | `src/modules/fraud/` |
| ATLAS-PEND-108 | Abierto | Dato faltante | Baja | Auth internos | No existe autoregistro ni gestión CRUD de `internal_users`/`platform_users` — hoy solo se crean por seed/migración manual y luego se les provisiona contraseña vía `POST /auth/provision-credentials`. Falta un flujo real de alta/baja de usuarios internos desde el panel de operaciones. | Operativamente, dar de alta a un nuevo analista de riesgo requiere intervención manual en base de datos. | Diseñar un módulo de gestión de usuarios internos (Fase 2, plataforma administrativa). | Backend/Operaciones | `src/modules/auth/`, futuro módulo de gestión de usuarios |
| ATLAS-PEND-109 | Bloqueante | Riesgo técnico | Alta | Validación de entrega | Este patch se implementó sin acceso a red ni a una base de datos PostgreSQL real (sandbox aislado). No se pudo ejecutar `yarn install`, `yarn test`, `yarn build`, ninguna migración, ni ningún smoke test contra un servidor real. Toda la validación se hizo con el compilador de TypeScript (`tsc --noEmit`) para los archivos que no dependen de paquetes nuevos, más revisión manual línea por línea. | El código puede compilar y ejecutar tal como está diseñado, pero **no está confirmado end-to-end** hasta que alguien lo corra con `yarn install` + Postgres real. | Antes de desplegar: `yarn install && yarn db:migration:up && yarn db:seed:up && yarn type-check && yarn test && yarn build && yarn smoke`. Ver `IMPLEMENTATION_REPORT.md` para el detalle exacto de qué se validó y qué no. | Backend | Todo el patch |

## 3. Pendientes de Fase 3 (BNPL) — sin cambios respecto al brief original

Los 8 pendientes de negocio originales de `PENDIENTES_ATLAS.md` (`ATLAS-PEND-001`..`008`: concurrencia de compras, plazo del 60%, calendario de mora, mecanismo de MDR, campos obligatorios de KYC, y 3 preguntas operativas) siguen abiertos y **no aplican todavía** porque el dominio BNPL (`purchases`, `installment-plans`, `credit-lines`, `merchants`, `payments`, `merchant-settlements`, `collections`) no se ha empezado a construir — eso es correcto y esperado dado el roadmap (Fase 1 → Fase 2 → Fase 3). No se deben cerrar por cuenta propia cuando llegue el momento de construir Fase 3; ver `PENDIENTES_ATLAS.md` §6.

## 4. Regla de cierre

Ningún pendiente de la tabla §2 debe cerrarse en un futuro patch sin dejar la fila actualizada a `Resuelto` con una breve nota de qué se hizo y dónde. No sobrescribir el historial: agregar filas nuevas en vez de borrar las resueltas, para mantener trazabilidad completa del proyecto.
