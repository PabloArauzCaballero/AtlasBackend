# Pending items — Atlas Backend

Registro vivo de pendientes técnicos y decisiones de producto abiertas. Se actualiza en el mismo
cambio que resuelve o abre un ítem — no se deja para después.

## Resueltos en esta intervención (Fase 1 y Fase 2 del plan de mejora)

| ID | Estado | Tipo | Prioridad | Área | Descripción | Resolución |
|---|---|---|---|---|---|---|
| ATLAS-TECH-001 | Resuelto | Regla técnica | Crítica | Migraciones | `20260626154044-create-atlas-user-intelligence-fraud-schema-v5-2-1.ts` duplicaba la creación de las 86 tablas ya cubiertas por `schema-part-0..9`, con el mismo timestamp. Umzug ordena alfabéticamente (`paths.sort()`), así que el monolito corría primero y la migración split fallaba con `relation already exists`. Bloqueaba `yarn db:migration:up` desde cero. | Archivo eliminado. Verificado por simulación del orden real de Umzug y `tsc` completo en verde. |
| ATLAS-TECH-002 | Resuelto | Deuda técnica | Media | Modelos Sequelize | 17 modelos duplicados en singular (`tenant.model.ts`, `customer.model.ts`, `fraud-case.model.ts`, etc.) no estaban importados en ningún lugar (`models/index.ts` ni repositorios). Código muerto que podía confundir a quien buscara el modelo "correcto". | 16 archivos eliminados (el 17°, `content-subscriber.model.ts`, ya no existía en `src/`, solo en `dist/` stale). `tsc --noEmit` y build completo verificados sin errores tras el borrado. |
| ATLAS-TECH-003 | Resuelto | Deuda técnica | Baja | Repo hygiene | `coverage/` (275 archivos) estaba trackeado en git; debería ser output generado. | Sacado del tracking (`git rm --cached`), agregado a `.gitignore` junto con `tmp-*.log`. |
| ATLAS-TECH-004 | Resuelto | Dato faltante | Alta | Seeds | 45 de 121 tablas (37%) no tenían ninguna fila en ningún seeder: el cliente demo sembrado tenía entidades "padre" sin ningún registro "hijo"/evento, imposibilitando probar en profundidad timelines, auditoría o explicabilidad de riesgo. | 38 de esas 45 tablas ahora tienen datos de demostración enlazados por FK real al cliente demo (`_id=1`), repartidos en 3 seeders nuevos: `20260706000000-seed-deep-graph-demo-data.ts`, `20260706010000-seed-external-provider-and-catalog-governance-demo-data.ts`, `20260706020000-seed-schema-constraint-notes.ts`. Se agregó `scripts/verify-seed-graph-integrity.ts` (`yarn db:seed:verify-graph`) que falla si alguna relación padre→hijo queda huérfana. |
| ATLAS-TECH-006 | Resuelto | Verificación pendiente | Alta | Migraciones/Seeds/Runtime | Fase 1 y Fase 2 solo se habían verificado con `tsc --noEmit`/build, nunca contra Postgres real. | 2026-07-08: corrido completo contra PostgreSQL real (`yarn db:migration:up`, `yarn db:seed:up`, `yarn start:dev`, `yarn smoke` — 69 llamadas OK — y `yarn test:coverage` — 82 suites / 773 tests OK). Detalle en `docs/testing/validacion-local-windows.md` §8. |
| ATLAS-SEC-001 | Resuelto | Vulnerabilidad | Crítica | Multitenant | Ningún guard central obligaba a que `x-tenant-id` coincidiera con el `tenantId` del JWT — un actor autenticado en el tenant A podía operar el tenant B cambiando el header. | `TenantGuard` (`src/common/guards/tenant.guard.ts`) agregado a los 17 controllers que reciben `x-tenant-id`; rechaza con 403 si el header no coincide con el token. Validado contra servidor real (smoke suite, ningún 403 inesperado). |
| ATLAS-RISK-001 | Resuelto | Deuda técnica | Media | Riesgo | El motor de riesgo (`RiskService`) calculaba scores con reglas heurísticas fijas sin identificarse como tal — riesgo de presentarlo como scoring financiero real. | Extraído a constantes explícitas (`risk-heuristic-v0.constants.ts`: `RISK_MODEL_CODE='risk_heuristic_v0'`). El endpoint de creación ahora devuelve `modelCode`/`modelVersion`/`rulesetVersion`. Sigue siendo heurística v0 — un motor de políticas versionado en base de datos queda fuera de este cambio (requeriría tablas/migraciones nuevas probadas contra Postgres real). |
| ATLAS-HYG-001 | Resuelto | Deuda técnica | Baja | Repo hygiene | `scripts/check-no-env-file.ts` rechazaba `.env.production.example` (template legítimo con placeholders) por un allowlist que solo aceptaba el nombre exacto `.env.example`. | Corregido para aceptar cualquier archivo terminado en `.example`. |
| ATLAS-PEND-009 | Resuelto | Deuda técnica / seguridad | Alta | Código ajeno mezclado | `src/modules/{advertising,content,scheduling,users}/`, `src/config/env.validation.ts`, `src/config/configuration.ts`, `src/database/database-bootstrap.service.ts` (registrado en un `src/database/database.module.ts` — un segundo archivo con el mismo nombre de clase que `sequelize.module.ts`, la fuente real) y la migración `.js` `20260705020000-schema-compatibility-and-premium-news.js` pertenecían a otro producto distinto ("Corazon Migrante Backend" — app de citas con terapeutas + CMS/publicidad: tablas `therapist_profiles`, `patient_profiles`, `appointments`, `content_publications`, `ads_campaigns`, etc.), no a Atlas. | 2026-07-08: confirmado que nada de este código estaba registrado en `app.module.ts` (ninguno de los 4 módulos, `ContentModule` incluido, aparecía importado) y que la migración foránea es `.js` mientras el runner solo globa `*.ts` (`src/database/migrate.ts`) — nunca corrió. Verificado con consulta directa a Postgres que ninguna tabla foránea existe en la base real. Eliminado todo el árbol completo. `tsc --noEmit`, `yarn lint` y `yarn test` (83 suites / 778 tests) verdes después del borrado. |
| ATLAS-OBS-001 | Resuelto | Feature incompleta | Media | Logging | `ArchivoLogMongoSyncService` sincronizaba `Archivo.log` hacia MongoDB, pero nada en el backend escribía ese archivo — el `Logger` de Nest solo imprimía a consola. | Se agregó `AppFileLogger` (`src/common/logging/app-file-logger.service.ts`), que extiende `ConsoleLogger` y además apila cada línea en `LOG_SYNC_FILE_PATH` sin bloquear el event loop. Conectado en `main.ts` vía `NestFactory.create(AppModule, { logger: new AppFileLogger() })`. Verificado en runtime real: `Archivo.log` se crea y captura el arranque completo. |

## Resueltos en la intervención del 2026-07-28 (catálogo de flujos de trabajo)

| ID | Estado | Tipo | Prioridad | Área | Descripción | Resolución |
|---|---|---|---|---|---|---|
| ATLAS-FLOW-001 | Resuelto | Dato faltante | Alta | Contrato con el frontend | El árbol de endpoints del proceso estándar (qué llamadas, en qué orden, con qué precondiciones y transiciones) no existía como dato: vivía repartido entre la prosa de `docs/endpoints/endpoints.md`, la lógica de `CustomerEligibilityService` y lo que cada cliente HTTP hubiera codificado por su cuenta. Cada consumidor reimplementaba el recorrido y se desincronizaba en silencio. | Módulo `workflow-catalog` + cinco tablas en `platform_ops` (`workflow_definitions`, `workflow_stages`, `workflow_steps`, `workflow_step_dependencies`, `workflow_transitions`), migración `20260728140000-create-workflow-catalog`, seeder productivo idempotente con el flujo REAL (`customer_credit_journey` v1: 22 etapas, 57 pasos, 18 dependencias, 33 transiciones) y 9 endpoints de consulta. Documentado en `docs/endpoints/workflow-catalog.md`. |
| ATLAS-FLOW-002 | Resuelto | Regla técnica | Alta | Gobierno | Nada detectaba que el proceso documentado dejara de corresponder a los endpoints expuestos: renombrar una ruta no rompía nada hasta que un cliente la llamaba. | `GET /operations/workflows/:code/consistency` compara cada paso con las rutas que ESTE proceso tiene montadas (`DiscoveryService`, no análisis de archivos) y con la máquina de estados del cliente. Complemento estático en `test/unit/workflow-catalog/customer-credit-workflow.seed-data.spec.ts`, que corre sin base de datos. Estado verificado el 2026-07-28: `in_sync`, 0 errores. |
| ATLAS-DOC-001 | Resuelto | Inconsistencia documental | Media | Documentación | `docs/endpoints/endpoints.md` describía `POST /customers/:customerId/eligibility/evaluate`, un endpoint que nunca existió. El cálculo lo hace `GET /customers/:customerId/eligibility`, que persiste la evidencia. | Documentación corregida y decisión explicitada: no se agrega un `POST` equivalente porque un segundo camino para el mismo cálculo permitiría que consulta y decisión divergieran. |
| ATLAS-DOC-002 | Resuelto | Inconsistencia documental | Baja | Consentimientos | `consents.schemas.ts` decía que `channel` y `countryCode` se aceptaban "para compatibilidad futura" cuando no estaban en el schema ni podían aplicarse (`consent_documents` no tiene esas columnas). | Comentario reemplazado por la razón real: un parámetro ignorado en silencio le haría creer al cliente que filtró cuando no lo hizo. Si el negocio los necesita, entran con su migración y su filtro. |
| ATLAS-DOC-003 | Resuelto | Dato faltante | Baja | Gobierno de datos | El seeder de narrativas reportaba 9 entidades catalogadas sin narrativa de negocio: las 3 de `credit`, `customer_eligibility_evaluations` y las 5 nuevas de `workflow_*`. | Agregadas `credit-lifecycle.fixtures.ts` y `workflow-catalog.fixtures.ts`. El seeder ahora reporta "Entidades catalogadas sin narrativa: ninguna" (139/139). |
| ATLAS-DOC-004 | Resuelto | Artefacto obsoleto | Media | Contrato | `docs/endpoints/openapi.yaml` estaba desactualizado: 197 rutas, sin crédito ni elegibilidad. | Regenerado con `yarn docs:openapi`: 249 rutas, incluidas las de crédito, elegibilidad y catálogo de flujos. |

## Resueltos en la auditoría integral del 2026-07-30

Detalle y evidencia en [auditoria-integral-2026-07-30.md](../audit/auditoria-integral-2026-07-30.md).

| ID | Estado | Tipo | Prioridad | Área | Descripción | Resolución |
|---|---|---|---|---|---|---|
| ATLAS-TECH-001 | Resuelto (regresión) | Regla técnica | Crítica | Migraciones | El monolito `20260626154044-create-atlas-user-intelligence-fraud-schema-v5-2-1.ts` **seguía en el repositorio** (en `main` y en la rama de trabajo) pese a estar dado por eliminado en 2026-07-08. Crea las mismas 86 tablas que `schema-part-0..9`, comparte prefijo de timestamp con `schema-part-0` y gana el orden alfabético de Umzug (`'c' < 's'`), así que `yarn db:migration:up` sobre una base vacía abortaba en la segunda migración. Provisionar un entorno nuevo era imposible, y dos jobs de CI no podían pasar. | Equivalencia verificada programáticamente (86/86 tablas, 244/244 FKs, 5/5 checks, 385/385 índices) y documentada en [migration-split-verification.md](../architecture/migration-split-verification.md); archivo eliminado. Nuevo gate `yarn check:migrations` (estático, sin base de datos) bloquea la regresión en el PR: colisión de tabla no idempotente, prefijo de timestamp repetido sin excepción documentada, migración sin `down` y nombre fuera de patrón. |
| ATLAS-SEC-003 | Resuelto | Vulnerabilidad | Crítica | external-data | Los 9 proveedores externos se siembran con `default_mode = 'mock_local'` y `toMode()` también caía a `mock_local`, así que un despliegue productivo que no fijara `${CODE}_MODE=production` **verificaba identidades y calculaba riesgo sobre payloads inventados por el propio adaptador**, persistidos como observaciones y features del cliente, sin fallar ni alertar. | `productionIntegrationBlockers` bloquea los modos simulados en producción (`*_MOCK_MODE_IN_PRODUCTION`) salvo `EXTERNAL_PROVIDERS_ALLOW_MOCK_IN_PRODUCTION=true` explícito. Aplica a ejecución y preview vía la maquinaria de políticas existente, y a `BankingQrService` con un portón propio (un QR de cobro simulado es un QR al que se transfiere dinero real). `mockBaseUrlFor` deja de inventar `localhost` en producción. El registro audita los modos al arrancar y los expone en readiness. |
| ATLAS-OPS-005 | Resuelto | Robustez | Alta | runtime-jobs / notifications | Dos colas crecían sin que nada las recogiera. (a) `NotificationBroadcastService` entrega en *fire-and-forget* (un broadcast puede targetear decenas de miles de destinatarios y hacerlo dentro del request lo haría durar minutos): si el proceso se reinicia a mitad de tanda, los mensajes ya creados quedan en `pending`/`sending` para siempre, persistidos y sin que nada vuelva a intentarlos. (b) `idempotency_keys` solo crecía — cada mutación con `X-Idempotency-Key` deja una fila y nada las borraba, degradando a la larga el propio `claimIdempotency` que protege cada comando. | Dos jobs nuevos con endpoint, entrada en el planificador, alerta Prometheus y pruebas: `POST /operations/jobs/retry-stuck-notifications` (reintenta por el MISMO orquestador que la entrega normal, que ya corta solo si el mensaje llegó a estado terminal, así que es seguro aunque otra instancia lo entregara entretanto) y `POST /operations/jobs/purge-idempotency-keys` (borra solo `completed`/`failed` anteriores a `RUNTIME_JOBS_IDEMPOTENCY_RETENTION_DAYS`; las `processing` no se tocan nunca porque podrían pertenecer a una petición en vuelo). |
| ATLAS-OPS-001 | Resuelto | Feature incompleta | Crítica | runtime-jobs | Los cinco jobs de fondo solo existían como endpoints HTTP: sin `@Cron`, sin `setInterval` y sin manifiesto de despliegue que los llamara. En producción el outbox nunca se despachaba, las sesiones caducadas nunca expiraban y las **políticas de retención de datos personales nunca se aplicaban**. | `RuntimeJobsSchedulerService` (opt-in por `RUNTIME_JOBS_SCHEDULER_ENABLED`, sin dependencia nueva) con elección de líder por Redis `SET NX PX`, fail-closed en producción sin lock, métrica `atlas_scheduled_job_runs_total` y alertas `AtlasScheduledJobFailing` / `AtlasRetentionJobNotRunning`. |
| ATLAS-SEC-004 | Resuelto | Vulnerabilidad | Alta | Logging | `AppFileLogger` redactaba la PII del ARCHIVO pero llamaba antes a `super.log(...)`, que imprimía el mensaje crudo por stdout — el canal que en un contenedor recoge el agregador de logs. Además stdout era texto humano sin `correlationId`/`traceId`. El filtro de excepciones agravaba el problema registrando `request.url` con su query string completa. | Con `LOG_FORMAT=json` (default en producción) stdout recibe la misma línea JSON redactada y correlacionada que el archivo. `sanitizeUrlForLog` conserva los nombres de los parámetros y descarta sus valores. |
| ATLAS-OPS-002 | Resuelto | Dato faltante | Alta | Observabilidad | `/health` leía `process.env['npm_package_version']`, que no existe al arrancar con `node dist/src/main.js`: producción respondía siempre `'0.1.0'` y no había forma de saber qué build estaba desplegado. | `src/config/build-info.ts` resuelve `APP_VERSION` → `npm_package_version` → `package.json` → `0.0.0-unknown`, y `/health` reporta además `commit` y `builtAt`. El `Dockerfile` los recibe por `--build-arg`. |
| ATLAS-OPS-003 | Resuelto | Artefacto faltante | Alta | Despliegue | No existía `Dockerfile`, `.dockerignore`, compose ni manifiesto alguno, pese a que el runbook describe un despliegue y el plan asume "la imagen de prod". El artefacto desplegable era, literalmente, indefinido. | `Dockerfile` multi-stage (deps → build → runtime) con `tini` como PID 1, usuario sin privilegios, solo dependencias de producción y `HEALTHCHECK` contra readiness; `.dockerignore` que deja fuera `.env`, historial y evidencia de smokes; `docker-compose.yml` con las mismas versiones de Postgres/Redis/Mongo que CI; job `docker-image` en CI. |
| ATLAS-OPS-004 | Resuelto | Robustez | Media | Ciclo de vida | Readiness seguía respondiendo 200 durante el apagado (cada despliegue tiraba las peticiones de esa ventana), el cliente Redis no lo cerraba nadie y no había techo de duración para una petición colgada. | `GracefulShutdownService` (readiness a 503 + espera `SHUTDOWN_DRAIN_MS` antes de cerrar), `RedisLifecycleService` (`quit()` con degradación a `disconnect()`) y `RequestTimeoutInterceptor` (`REQUEST_TIMEOUT_MS`, exime `/metrics` y los probes). |
| ATLAS-SEC-005 | Resuelto | Endurecimiento | Media | Auth | El token de acceso se firmaba y verificaba sin `iss` ni `aud`, y el mismo `JWT_ACCESS_TOKEN_SECRET` lo usa también el probe de `systems-health.service.ts`. Cualquier token futuro firmado con ese secreto para otro propósito era un token de sesión en potencia. | `accessTokenSignOptions`/`accessTokenVerifyOptions` (`jwt-claims.util.ts`) centralizan `JWT_ISSUER`/`JWT_AUDIENCE` en los 8 puntos que firman o verifican (runtime, scripts, smokes y e2e). **Al desplegar, los tokens de acceso previos se rechazan durante como mucho `JWT_ACCESS_TOKEN_EXPIRES_IN`; los refresh tokens son opacos y los clientes renuevan solos.** |
| ATLAS-HYG-002 | Resuelto | Deuda técnica | Media | Dependencias | `joi` estaba en `dependencies` sin ninguna importación en `src/`, `scripts/` ni `test/`: superficie de cadena de suministro gratuita y contradicción con la regla de una sola librería por responsabilidad (`zod` ya valida). También quedaba en la raíz `fix-atlas-backend-quality.cjs`, un codemod de un solo uso ya aplicado. | Ambos eliminados. |

## Resueltos en el cierre del 2026-07-31 (credenciales y guardas de adaptador)

| ID | Estado | Tipo | Prioridad | Área | Descripción | Resolución |
|---|---|---|---|---|---|---|
| ATLAS-SEC-006 | Resuelto | Vulnerabilidad | Alta | external-data | Cinco de los ocho adaptadores externos (`digital-trust`, `facebook-meta`, `qr-generic`, `telco-generic`, `whatsapp`) NO tenían guarda propia de `production`/`sandbox`, y `BankingGenericAdapter.execute` (verificación de transferencias) tampoco. El único freno era `productionIntegrationBlockers`, que depende de que `${CODE}_REAL_INTEGRATION_IMPLEMENTED` diga la verdad — una variable de entorno por sistema de honor. Puesta en `true` sin integración, esos adaptadores servían un payload FABRICADO etiquetado como producción: evidencia KYC inventada persistida como features del cliente, y un `VERIFIED` de transferencia que libera producto contra un dinero que nadie recibió. | Los ocho adaptadores se niegan ahora por su cuenta con `*_REAL_INTEGRATION_NOT_CONFIGURED`, sin depender de que la configuración sea honesta. Fijado en `test/unit/external-data/adapters-production-guard.spec.ts` (16 casos: los 8 en `production` y los 8 en `sandbox`). |
| ATLAS-DOC-005 | Resuelto | Dato faltante | Alta | Configuración | 8 de las 14 claves que `PRODUCTION_CREDENTIAL_REQUIREMENTS` exige (`QR_GENERIC_BASE_URL`, `BANKING_GENERIC_BASE_URL`, `TELCO_GENERIC_BASE_URL`, `DIGITAL_TRUST_GENERIC_BASE_URL`, `META_FACEBOOK_APP_ID/_SECRET/_REDIRECT_URI`, `WHATSAPP_PROVIDER`) no estaban en ninguna plantilla `.env`, y ningún gate lo detectaba: `check:env-example` solo valida las variables tipadas en `env.schema.ts`, mientras que estas se leen por `process.env` directo. Un operador no sabía que existían hasta que el proceso se negaba a arrancar. | Añadidas a ambas plantillas y **`check:env-example` extendido** para exigir que toda clave de `PRODUCTION_CREDENTIAL_REQUIREMENTS` esté en las dos (verificado quitando una a mano: el gate falla). Documento nuevo [credenciales-requeridas.md](../config/credenciales-requeridas.md) con las credenciales de las 4 áreas (bloqueantes, proveedores externos, notificaciones, plataforma), quién provee cada una y qué se apaga sin ella. |

## Pendientes abiertos

| ID | Estado | Tipo | Prioridad | Área | Pendiente | Impacto si no se resuelve | Acción requerida | Responsable |
|---|---|---|---|---|---|---|---|---|
| ATLAS-FLOW-003 | Asumido deliberadamente | Decisión de producto | Baja | workflow-catalog | El informe de consistencia reporta ~66 avisos `ROUTE_NOT_MAPPED`: rutas de dominios que el flujo cubre (`auth`, `customers`, `customer-onboarding`, `operations`, `external-data`) que NO forman parte del recorrido estándar — recuperación de contraseña, notificaciones, tokens de dispositivo, administración de proveedores, jobs. | Ninguno funcional: son avisos, no errores, y `status` sigue siendo `in_sync`. El riesgo es que el ruido esconda un aviso legítimo. | Cuando producto defina flujos adicionales (recuperación de cuenta, gestión de notificaciones), sembrarlos como definiciones propias con su `workflow_code`. La estructura ya lo soporta sin cambios de esquema. | Producto / Backend |
| ATLAS-TECH-005 | Abierto | Dato faltante | Baja | Seeds | 5 tablas siguen sin fila a propósito: `auth_refresh_tokens`, `system_action_logs`, `system_test_runs`, `system_test_step_runs` (artefactos generados en runtime/al correr tests reales — sembrarlos con datos falsos les quita valor), y `external_oauth_connections` (sin consumidor real todavía). | Ninguno funcional; son artefactos de ejecución, no catálogos de referencia. | Ninguna acción salvo que producto decida que alguno deba tener datos fijos de referencia. | Equipo backend |
| ATLAS-TECH-007 | Asumido temporalmente | Regla técnica | Media | schema-management | Aprobar un cambio en `PATCH /operations/schema/change-log/:id/approve` registra la decisión en `schema_change_log` pero **no ejecuta el DDL físico** (`CREATE TABLE` real). Decisión deliberada (Opción C híbrida aprobada por el usuario): el DDL real sigue saliendo por migraciones Sequelize revisadas en PR; la API gobierna el catálogo y la auditoría. | Si alguien asume que aprobar crea la tabla física, habrá divergencia entre catálogo y BD real. El README del módulo y el service lo documentan explícitamente. | Cuando se decida habilitar ejecución física (Fase 4C/4D), implementar el executor DDL con: transacción, validación contra catálogo, timeout de locks, y prueba en staging obligatoria. Hasta entonces, mantener la disciplina de migraciones. | Arquitectura / Usuario |
| ATLAS-SEC-002 | Congelado con trinquete | Deuda técnica | Baja | Controllers | Medido el 2026-07-30: **26 controllers / 129 usos** de `@Headers('x-tenant-id')` en vez de `@CurrentTenant()` (`src/common/decorators/current-tenant.decorator.ts`). `tenantIdFromHeader(header, user)` y el decorador son semánticamente idénticos, así que la migración es mecánica. La brecha de seguridad real ya la cierra `TenantGuard`. | Ninguno funcional. Migrar los 26 de golpe obligaría a tocar también sus 26 specs (que llaman a los handlers posicionalmente) y a revalidar cada endpoint con alcance de tenant de un backend fintech, a cambio de cero beneficio funcional. | Se mantiene la decisión de migrar al tocar cada controller por otra razón, pero ya **no depende de la disciplina**: `yarn check:tenant-header` (en CI) congela `.tenant-header-baseline.json` y falla si un archivo nuevo introduce el patrón o si uno del baseline suma usos. Al migrar, bajar el piso con `--update-baseline`. | Backend |

## Marcas usadas
El seguimiento debe quedar en este registro o en issues formales; no se deben abrir nuevos pendientes como comentarios inline.
